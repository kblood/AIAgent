<Window x:Class="AIAgentTest.Views.MCPServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MCP Server" Width="400" Height="300"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Server name -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}" Margin="0,5" Padding="5"/>
        
        <!-- Server URL -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="URL:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Url}" Margin="0,5" Padding="5"/>
        
        <!-- Server type -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Type:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding AvailableTypes}" 
                 SelectedItem="{Binding Type}" Margin="0,5" Padding="5"/>
        
        <!-- Enabled state -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Enabled:" VerticalAlignment="Center"/>
        <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding IsEnabled}" 
                 VerticalAlignment="Center" Margin="0,5"/>
        
        <!-- Help text based on type -->
        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                  Text="Filesystem MCP Server: Provides access to the file system. URL should be the server address (e.g., http://localhost:3000)."
                  TextWrapping="Wrap" Margin="0,10" Opacity="0.7">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="database">
                            <Setter Property="Text" Value="Database MCP Server: Provides access to database operations. URL should be the server address with appropriate authentication."/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="custom">
                            <Setter Property="Text" Value="Custom MCP Server: Provides custom tool capabilities. URL should be the full server address with protocol."/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" 
                   Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" IsCancel="True" Padding="15,5" Margin="0,0,10,0"
                   Click="CancelButton_Click"/>
            <Button Content="Save" IsDefault="True" Padding="15,5" 
                   Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>